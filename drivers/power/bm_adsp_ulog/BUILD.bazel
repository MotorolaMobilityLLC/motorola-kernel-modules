load("//soc-repo:kleaf-scripts/moto/moto_modules_define.bzl", "moto_ddk_module")

moto_ddk_module(
    name = "bm_adsp_ulog",
    srcs = ([
        "bm_adsp_ulog.c",
    ]),
    deps_ext = [
        "//soc-repo:drivers/soc/qcom/qti_pmic_glink",
        "//soc-repo:kernel/trace/qcom_ipc_logging",
    ],
)
