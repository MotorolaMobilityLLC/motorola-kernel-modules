load("//build/bazel_mgk_rules:mgk_ko.bzl", "define_mgk_ko")

package(
    default_visibility = [
        "//visibility:public",
    ],
)

define_mgk_ko(
    name = "mm_iosched",
    srcs = glob([
        "*.c",
        "*.h",
        "Kbuild",
        "Makefile",
    ]) + [
        "//motorola/kernel/modules/drivers/moto_sched:sched_headers",
    ],
    deps = [
        "//motorola/kernel/modules/drivers/moto_sched:moto_sched",
    ],

    outs = ["mm_iosched.ko"],
)
