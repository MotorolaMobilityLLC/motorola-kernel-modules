load("//soc-repo:kleaf-scripts/moto/moto_modules_define.bzl", "moto_ddk_module")

moto_ddk_module(
    name = "watchdogtest",
    srcs = ([
        "watchdogtest.c",
    ]),
    deps_ext = [
        "//soc-repo:drivers/soc/qcom/qcom_wdt_core",
    ],
)
